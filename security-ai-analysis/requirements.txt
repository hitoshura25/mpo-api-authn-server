# Core ML packages (pinned to working versions)
transformers==4.36.2   # Pinned - working version from local testing
torch==2.1.2          # Pinned - stable CPU version
accelerate==0.24.1    # For efficient model loading
datasets==2.14.6      # For dataset handling

# OLMo specific (required for OLMo model loading)
git+https://github.com/allenai/OLMo.git  # AI2's official OLMo package

# Data processing (pinned versions)
pyyaml==6.0.1
pandas==2.1.4
numpy==1.24.4

# System monitoring and debugging
psutil==5.9.8         # For memory monitoring in GitHub Actions

# Utilities
python-dotenv==1.0.0  # For environment variables
tqdm==4.66.1          # For progress bars

# Tokenization dependencies (pinned)
sentencepiece==0.1.99  # Some models need this
protobuf==4.25.1       # For some model formats
tokenizers==0.15.0     # Core tokenization library

# For Git integration (used in narrativizer)
gitpython==3.1.40      # For collecting git history fixes
