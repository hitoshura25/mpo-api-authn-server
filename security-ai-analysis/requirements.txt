# Core ML packages
transformers>=4.36.0
torch>=2.0.0
accelerate>=0.24.0  # For efficient model loading
datasets>=2.14.0    # For dataset handling

# OLMo specific (optional - will work without, but better with)
# ai2-olmo>=0.2.0  # Uncomment if AI2's OLMo package becomes available
# hf-olmo>=0.1.0   # Uncomment if available

# Data processing
pyyaml>=6.0
pandas>=1.5.0
numpy>=1.24.0

# Utilities
python-dotenv>=1.0.0  # For environment variables
tqdm>=4.65.0         # For progress bars

# Optional but recommended for better tokenization
sentencepiece>=0.1.99  # Some models need this
protobuf>=3.20.0      # For some model formats

# For Git integration (used in narrativizer)
gitpython>=3.1.30     # For collecting git history fixes
