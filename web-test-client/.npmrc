# Configure npm to use GitHub Packages for @vmenon25 scoped packages
# This enables pulling both staging packages (GitHub Packages) and production packages (npm)
@vmenon25:registry=https://npm.pkg.github.com

# For production builds, packages will be available on npm registry
# registry=https://registry.npmjs.org

# GitHub Packages authentication (required for staging packages)
# Will be set by CI or manually: //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}